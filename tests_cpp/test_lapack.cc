#include <lapack_interface.h>
#include <matrix.h>
#include <vector.h>

#include <iostream>

using namespace Tombino_bla;
using namespace std;

int main() {
  // create a vector of size 5
  Vector<double> x(5);
  // fill it with 1
  x = 1;
  // print it
}
